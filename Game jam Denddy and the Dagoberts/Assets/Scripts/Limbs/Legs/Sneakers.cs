using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sneakers : Leg
{
    // Start is called before the first frame update
    void Start()
    {
        spd = 20;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
